import 'package:flutter/material.dart';

class SvenNScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Sven's Seite"),
      ),
      body: Container(
        child: Text("SVEN NOWAK"),
      ),
    );
  }
}
